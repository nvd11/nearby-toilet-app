
## 2026-03-23 15:57 UTC - RAG Evaluator Project (Standard Chartered Interview)
- **Database Redesign**: Removed hard Foreign Keys (FKs) for enterprise scalability. Decoupled pipeline into `inference_run_history` and `evaluation_job_history` so one inference run can have multiple evaluations. Added `retrieval_time` to track latency granularly. Renamed `dataset_name` to `batch_name` for synthetic data lineage.
- **Phase 3 (Golden Dataset Generation)**: Designed Synthetic Ground Truth generator using a "Teacher" LLM to prevent data contamination. Documented with Mermaid UML/Sequence diagrams in `phase3_golden_dataset_generation.md`.
- **Asymmetric Compute Strategy**: Split LLM roles in settings for cost/quality balance: `LLM_INFERENCE_MODEL` (gemini-2.5-pro), `LLM_JUDGE_MODEL` (gemini-2.5-pro), `LLM_TEACHER_MODEL` (gemini-3.1-pro-preview).
- **Structured Output**: Adopted `with_structured_output(QA_Pair)` (Pydantic) over `JsonOutputParser` for 100% reliable JSON generation in LCEL chains.
- **Code Polish**: Removed emojis from `agent_runner.py` logs per Boss's request for a more serious/enterprise tone.
