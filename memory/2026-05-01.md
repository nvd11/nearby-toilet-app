
## 2026-05-01: Oracle Cloud Registration & Setup
- Boss successfully registered for an Oracle Cloud Free Tier account (Home Region: ap-singapore-1).
- Set up an OCI Service Account (`alice-sa`) and added it to the `Administrators` group.
- Configured local `oci-cli` with the generated private key stored securely at `/home/gateman/.openclaw/workspace/alice-sa.pem`.
- Created and packaged a new AgentSkill: `oci-manager` (located in `skills/oci-manager`) to automate Oracle Cloud resource management and potentially poll for Free Tier ARM instances.
\n- **Lesson Learned (Clash/Mihomo)**: Always execute the syntax validation command (e.g., `mihomo -t -f config.yaml`) before restarting the service to prevent breaking the proxy setup.
\n- **Preference**: Use HK Timezone (UTC+8) when communicating with the Boss.
\n- **Environment**: Boss's primary GCP Project is `jason-hsbc`.
\n- **Investigation**: At 11:48 AM HK time, an SSH user (from IP 120.235.167.134, likely Boss) switched to root (sudo su -), stopped the x-ui service manually, and then disconnected at 11:48:02.

## 2026-05-01: Tailscale & Home Network Setup
*   **Network Context**: Boss's home broadband is China Mobile (CGNAT, no public IP). DDNS won't work, so we are using Tailscale for intranet penetration.
*   **GCP Node**: Installed Tailscale on the GCP VM (`tf-vpc0-subnet0-openclaw`, Tailnet IP: `100.94.13.17`).
*   **StarFive Board (RISC-V)**: Attempted Tailscale install, but failed because the custom kernel (`6.12.5-starfive`) lacks the `tun` module. Decided to use `moon` as a Bastion Host instead.
*   **Moon Board (Radxa, radxa-cubie-a7a)**: Successfully added to Boss's Tailnet (`gateman56@gmail.com`) under IP `100.115.214.26`.
*   **Security Incident/Cleanup**: Discovered `moon` was previously in colleague Jayden's Tailnet (`thisisjayden@`), along with other devices. Boss used `--force-reauth` to reclaim `moon` into his own private Tailnet.
*   **Next Steps**: Waiting for SSH credentials for `moon` to test the connection and jump to the StarFive board.
