
## 2026-05-02: Local Network Proxy Management & Cloudflare Worker VLESS Deployment
*   **StarFive Proxy Update**: Boss requested to check the proxy node on the StarFive board (`10.0.1.227`). Accessed it via the `moon` jump host (`100.115.214.26`), discovered it was using `CF-亚太翻墙神机` (which was failing), and successfully switched it to `Moon-SG-AWS` using the local Clash API (port 9090, secret: 123456).
*   **Cloudflare Worker Deployment**: The `CF-亚太翻墙神机` node was failing (returning HTTP 522). Boss provided a Cloudflare API Token to fix it.
    *   Created a new Cloudflare Worker project (`cf-worker-vless`) using the `wrangler` CLI.
    *   Boss claimed his `workers.dev` subdomain (`gateman56.workers.dev`).
    *   Wrote and deployed the VLESS Worker code (`index.js`).
    *   The deployment automatically bound to the existing custom domain `vpn.jpgcp.cloud`, perfectly matching the SNI/Host in Boss's existing `CF-亚太翻墙神机` clash config.
    *   Result: The VLESS proxy on Cloudflare was fully restored and the `CF-亚太翻墙神机` node is functional again. The fixed UUID for the VLESS worker is `28661606-f836-460f-87ad-1b7248a455dc`.
