
## 2026-05-03 Updates
- **Infrastructure Update (Distributed Browser Hijacking):** Boss migrated the "Muscle" target machine. It is no longer the Arch Linux laptop. It is now a dedicated GCP VM named "Alice" (IP: `100.94.13.17`) running XRDP + XFCE4. Chrome debugging port is exposed at `localhost:9222` for Playwright CDP connections.
- **Automation Lesson (Playwright on CSDN):** Successfully automated CSDN article publishing over SSH on the new GCP VM using Playwright. Discovered that injecting text via `page.keyboard.insert_text()` into CSDN's Markdown editor (CodeMirror) loses newline formatting (`\n`). A clipboard paste approach or manual intervention is required for perfect markdown rendering on that specific site.
- **Gateway Diagnostics:** Investigated a Gateway hang issue on the Moon server (`100.115.214.26`). Found that `openclaw-gateway` was not crashing but was hanging on a stale TCP connection to the local proxy (`10.0.1.227:7890`). Remained read-only and did not restart the service, adhering strictly to the 2026-05-03 MEMORY.md safety rule.
