- **GCP Region Preference:** Boss explicitly specified to use `europe-west2` for ALL GCP resources (Dataflow, Cloud Run, Artifact Registry, etc.) due to organization policy.

- **Beam YAML Orchestration Service:** Built and deployed a Cloud Run service (`beam-yaml-orchestrator`) to dynamically trigger Top-Level Apache Beam YAML pipelines.
- **Architecture Choice:** Used "Option B" (direct SDK execution in Cloud Run) because Flex Templates were blocked by corporate policy issues.
- **Key Technical Findings:**
  - **Beam Version:** Upgraded to `apache-beam[gcp,yaml]==2.71.0` for Python 3.12 compatibility. The `yaml` extra is required to avoid `jinja2` missing errors.
  - **CLI Args change:** In newer Beam versions, the arg changed from `--yaml_file` to `--yaml_pipeline_file`.
  - **Cloud Run Background Tasks:** Because Beam job submission (DAG parsing and Staging GCS upload) takes 15-30 seconds, running it synchronously blocks the API and causes `504 Gateway Timeout` if using certain patterns. Implemented FastAPI `BackgroundTasks` with `subprocess.run()`.
  - **CPU Throttling:** Cloud Run must be deployed with `--no-cpu-throttling` for FastAPI background tasks to continue executing after the HTTP response is sent.
  - **Permissions:** The Cloud Run default compute service account needed `roles/storage.admin` to upload staging files to `gs://jason-hsbc-dataflow/temp` (fixed a 403 error).
- **GCP Region Rule:** Boss explicitly requested ALL GCP resources be deployed to `europe-west2` due to organization policy. We migrated from GCR to Artifact Registry (`my-docker-repo` in `europe-west2-docker.pkg.dev`).

- **Critical Incident & Post-Mortem (18:43 UTC): Channel Lockout on #gcp-dataflow:**
  - Diagnosed why I was unresponsive to Boss's messages. Discovered `lane wait exceeded` for `session:agent:main:slack:channel:c0anxtj30dt`.
  - **Root Cause:** Executed a long-running, blocking shell task (e.g. streaming deployment logs or polling deployment status) on the main channel lane. OpenClaw processes channel messages serially; my stalled turn caused a traffic jam, locking out all subsequent user messages.
  - **Action Taken:** Wrote a permanent rule into `MEMORY.md` to never block the main communication channel with synchronous polling. Will enforce the use of `sessions_spawn` (sub-agents) or background `exec` + yielding turns to ensure the main thread stays highly responsive.
